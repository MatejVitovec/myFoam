FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      faSolution;
}

solvers
{
    h
    {
        solver          smoothSolver;
        smoother        symGaussSeidel;
        tolerance       1e-10;
        relTol          0;
    }
}

relaxationFactors
{
    fields
    {
    }

    equations
    {
    }
}